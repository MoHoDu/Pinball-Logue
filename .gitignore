# 1. Logs (로그 파일)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 2. Dependencies (패키지 의존성 파일)
node_modules/
.pnp
.pnp.js

# 3. Build outputs (빌드 결과물 폴더)
dist/
build/

# 4. Environment variables (환경 변수 파일 - 보안상 매우 중요!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 5. Editor / OS files (에디터 및 운영체제 자동 생성 파일)
.vscode/
.idea/
.DS_Store
Thumbs.db

# 6. TypeScript cache (타입스크립트 캐시)
*.tsbuildinfo