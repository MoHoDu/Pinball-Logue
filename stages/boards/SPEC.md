---
title: 차원 독립 보드 표현과 2D 목업 명세
summary: 같은 보드 설정을 2D·3D 표현 어댑터가 소비하도록 분리하고 현재 2D 목업의 시점 각도와 편집 기준을 정의한다.
document_type: feature-spec
scope: stages/boards
status: approved
read_when:
  - 보드 시점, 형태 또는 2D·3D 표현 어댑터를 변경할 때
  - 보드 설정 Resource와 목업 화면을 검증할 때
---

# 차원 독립 보드 표현과 2D 목업

- 상태: approved
- 관련 결정: `DEC-20260728-02`
- 승인 기록: 2026-07-28 사용자가 첨부 핀볼 레퍼런스의 시점과 2D 목업 기준을 지정함

## 표현 방향

- 최종 그래픽의 2D·3D 선택은 미확정이다.
- 보드 형상·앵커·게임 규칙 데이터는 `Node2D`·`Node3D` 타입을 참조하지 않는다.
- 현재 목업은 `BoardMockup2D`가 담당한다. 미래 `BoardView3D`는 같은 보드 설정과 공개 상태를 소비하는 별도 어댑터로 추가한다.
- 런타임에서 2D와 3D를 동시에 유지하거나 토글하는 기능은 최종 방향 승인 전 구현하지 않는다.

## 기본 시점

- 첨부 레퍼런스처럼 보드 상단이 좁고 하단이 넓게 보이는 경사 원근을 사용한다.
- `view_angle_degrees` 기본값은 보드 평면 기준 58°다. 90°에 가까울수록 탑다운 직사각형에 가까워지고 값이 낮을수록 상단 폭이 좁아진다.
- 2D 목업은 이 각도를 사다리꼴 상단 폭과 세로 압축으로 변환한다.
- 미래 3D 어댑터는 같은 값을 카메라 또는 보드 기울기 설정으로 해석하되 시각 결과가 2D 기준과 유사한지 비교한다.

## 편집 가능한 설정

기본 설정은 `stages/boards/default_board_view_config.tres`에 둔다.

| 속성 | 기본값 | 용도 |
| --- | --- | --- |
| `view_angle_degrees` | 58° | 레퍼런스 기준 보드 시점 |
| `board_size` | 540×500 px | 2D 목업의 기준 폭·길이 |
| `rail_width` | 18 px | 외곽 레일 두께 |
| 색상 속성 | 기본 목업 팔레트 | 보드·레일·범퍼·강조 표시 |

- 모든 값은 Godot Inspector에서 수정하고 Resource를 복제해 비교 테스트할 수 있다.
- 설정 검증은 각도 20~85°, 양수 보드 크기와 레일 두께를 요구한다.

## 수용 기준

1. 기본 설정에서 상단이 좁고 하단이 넓은 58° 2D 보드 목업이 웨이브·보스 화면에 표시된다.
2. `.tres`의 `view_angle_degrees`를 바꾸면 코드 변경 없이 목업의 상단 폭과 세로 압축이 달라진다.
3. 잘못된 각도·크기는 검증 오류를 반환한다.
4. 보드 설정과 진행 도메인은 `Node2D`·`Node3D`를 참조하지 않는다.
5. 웨이브·보스 화면 전환과 진행 상태는 기존과 동일하게 유지된다.
